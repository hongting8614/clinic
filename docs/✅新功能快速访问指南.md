# 🚀 新功能快速访问指南

## 📱 页面路由配置

所有新页面已添加到 `pages.json`，可以通过以下路径访问：

### 1. 库存查询页面
```javascript
uni.navigateTo({
  url: '/pages-sub/stock/query'
})
```
- **路径**: `pages-sub/stock/query.vue`
- **标题**: 库存查询
- **功能**: 多维度库存查询、批次信息展示、统计汇总

### 2. 入库单报表
```javascript
uni.navigateTo({
  url: '/pages-sub/report/inbound'
})
```
- **路径**: `pages-sub/report/inbound.vue`
- **标题**: 入库单报表
- **功能**: 入库数据统计、时间范围筛选、导出功能

### 3. 库存报表
```javascript
uni.navigateTo({
  url: '/pages-sub/report/stock'
})
```
- **路径**: `pages-sub/report/stock.vue`
- **标题**: 库存报表
- **功能**: 库存概况统计、状态分析、导出功能

---

## 🎯 功能入口建议

### 方案一：在库存管理页面添加入口

修改 `pages/stock/index.vue`，添加快捷入口：

```vue
<view class="quick-actions">
  <view class="action-card" @tap="goStockQuery">
    <text class="action-icon">🔍</text>
    <text class="action-text">库存查询</text>
  </view>
  <view class="action-card" @tap="goStockReport">
    <text class="action-icon">📊</text>
    <text class="action-text">库存报表</text>
  </view>
</view>
```

```javascript
methods: {
  goStockQuery() {
    uni.navigateTo({
      url: '/pages-sub/stock/query'
    })
  },
  goStockReport() {
    uni.navigateTo({
      url: '/pages-sub/report/stock'
    })
  }
}
```

### 方案二：在报表页面添加入口

修改 `pages/record/index.vue` 或 `pages-sub/report/index.vue`，添加报表入口：

```vue
<view class="report-list">
  <view class="report-item" @tap="goInboundReport">
    <view class="report-icon">📦</view>
    <view class="report-info">
      <text class="report-title">入库单报表</text>
      <text class="report-desc">查看入库统计数据</text>
    </view>
    <text class="report-arrow">›</text>
  </view>
  
  <view class="report-item" @tap="goStockReport">
    <view class="report-icon">📊</view>
    <view class="report-info">
      <text class="report-title">库存报表</text>
      <text class="report-desc">查看库存概况统计</text>
    </view>
    <text class="report-arrow">›</text>
  </view>
</view>
```

```javascript
methods: {
  goInboundReport() {
    uni.navigateTo({
      url: '/pages-sub/report/inbound'
    })
  },
  goStockReport() {
    uni.navigateTo({
      url: '/pages-sub/report/stock'
    })
  }
}
```

### 方案三：在入库单列表页添加报表入口

修改 `pages-sub/in/list.vue`，在顶部添加报表按钮：

```vue
<view class="header-actions">
  <view class="action-btn" @tap="goInboundReport">
    <text class="btn-icon">📊</text>
    <text class="btn-text">查看报表</text>
  </view>
</view>
```

```javascript
methods: {
  goInboundReport() {
    uni.navigateTo({
      url: '/pages-sub/report/inbound'
    })
  }
}
```

---

## 🎨 UI 组件示例

### 快捷入口卡片样式

```scss
.quick-actions {
  display: flex;
  gap: 20rpx;
  padding: 20rpx 30rpx;
  
  .action-card {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15rpx;
    padding: 40rpx 20rpx;
    background: white;
    border-radius: 16rpx;
    box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
    
    .action-icon {
      font-size: 48rpx;
    }
    
    .action-text {
      font-size: 26rpx;
      color: #323233;
    }
  }
}
```

### 报表列表样式

```scss
.report-list {
  padding: 20rpx 30rpx;
  
  .report-item {
    display: flex;
    align-items: center;
    padding: 30rpx;
    background: white;
    border-radius: 16rpx;
    margin-bottom: 20rpx;
    box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
    
    .report-icon {
      width: 80rpx;
      height: 80rpx;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 48rpx;
      background: linear-gradient(135deg, #f7f8ff 0%, #f0f1ff 100%);
      border-radius: 16rpx;
      margin-right: 25rpx;
    }
    
    .report-info {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 8rpx;
      
      .report-title {
        font-size: 32rpx;
        font-weight: bold;
        color: #323233;
      }
      
      .report-desc {
        font-size: 24rpx;
        color: #969799;
      }
    }
    
    .report-arrow {
      font-size: 40rpx;
      color: #c8c9cc;
    }
  }
}
```

---

## 📋 功能使用说明

### 库存查询
1. 点击进入库存查询页面
2. 使用搜索框输入药材名称/规格/批号
3. 或使用扫码功能快速查询
4. 可通过分类、库存状态、效期状态进行筛选
5. 查看统计信息和批次详情

### 入库单报表
1. 点击进入入库单报表页面
2. 选择时间范围（支持快速选择或自定义）
3. 可选择供应商和操作人进行筛选
4. 点击"生成报表"查看统计数据
5. 可导出Excel/PDF或打印（功能预留）

### 库存报表
1. 点击进入库存报表页面
2. 选择药材分类、库存状态、效期状态
3. 点击"生成报表"查看统计数据
4. 查看库存概况和详细列表
5. 可导出Excel/PDF或打印（功能预留）

---

## 🔧 测试路径

### 开发工具测试
在微信开发者工具中，可以通过以下方式测试：

1. **直接访问**：
   - 在调试器中输入：`uni.navigateTo({url: '/pages-sub/stock/query'})`
   - 在调试器中输入：`uni.navigateTo({url: '/pages-sub/report/inbound'})`
   - 在调试器中输入：`uni.navigateTo({url: '/pages-sub/report/stock'})`

2. **添加临时入口**：
   在首页 `pages/index/index.vue` 添加测试按钮：
   ```vue
   <view class="test-buttons">
     <button @tap="testStockQuery">测试库存查询</button>
     <button @tap="testInboundReport">测试入库报表</button>
     <button @tap="testStockReport">测试库存报表</button>
   </view>
   ```

---

## 📊 数据流说明

### 库存查询数据流
```
用户输入/筛选 → 前端防抖 → 调用云函数 stock.query 
→ 返回数据 → 前端展示 → 点击查看详情
```

### 报表数据流
```
选择筛选条件 → 点击生成报表 → 调用云函数 reports.inboundReport/stockReport
→ 返回统计数据 → 前端展示表格 → 导出功能
```

---

## ⚠️ 注意事项

1. **云函数支持**：
   - 当前使用模拟数据
   - 需要创建对应的云函数：`stock`、`reports`
   - 云函数需要支持相应的 action

2. **权限控制**：
   - 建议添加权限验证
   - 敏感数据需要脱敏处理

3. **性能优化**：
   - 大数据量时建议使用分页
   - 考虑添加数据缓存

4. **导出功能**：
   - Excel/PDF导出功能已预留
   - 需要引入相应的导出库实现

---

## 🎉 快速开始

### 最简单的测试方法

1. 在 `pages/index/index.vue` 的首页添加测试入口：

```vue
<template>
  <view class="container">
    <!-- 其他内容 -->
    
    <!-- 新功能测试入口 -->
    <view class="new-features">
      <view class="section-title">🆕 新功能</view>
      <view class="feature-grid">
        <view class="feature-card" @tap="goStockQuery">
          <text class="feature-icon">🔍</text>
          <text class="feature-name">库存查询</text>
        </view>
        <view class="feature-card" @tap="goInboundReport">
          <text class="feature-icon">📦</text>
          <text class="feature-name">入库报表</text>
        </view>
        <view class="feature-card" @tap="goStockReport">
          <text class="feature-icon">📊</text>
          <text class="feature-name">库存报表</text>
        </view>
      </view>
    </view>
  </view>
</template>

<script>
export default {
  methods: {
    goStockQuery() {
      uni.navigateTo({
        url: '/pages-sub/stock/query'
      })
    },
    goInboundReport() {
      uni.navigateTo({
        url: '/pages-sub/report/inbound'
      })
    },
    goStockReport() {
      uni.navigateTo({
        url: '/pages-sub/report/stock'
      })
    }
  }
}
</script>

<style lang="scss" scoped>
.new-features {
  padding: 20rpx 30rpx;
  
  .section-title {
    font-size: 32rpx;
    font-weight: bold;
    color: #323233;
    margin-bottom: 20rpx;
  }
  
  .feature-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20rpx;
    
    .feature-card {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 15rpx;
      padding: 40rpx 20rpx;
      background: white;
      border-radius: 16rpx;
      box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
      
      .feature-icon {
        font-size: 48rpx;
      }
      
      .feature-name {
        font-size: 24rpx;
        color: #646566;
      }
    }
  }
}
</style>
```

2. 保存后刷新小程序，即可在首页看到新功能入口

3. 点击相应卡片即可进入对应页面

---

## 📞 技术支持

如有问题，请查看：
- `docs/✅用户反馈全部完成_2025-11-08.md` - 完整功能说明
- 各页面源码中的注释

**开发完成日期**：2025-11-08  
**版本**：v1.0.0


