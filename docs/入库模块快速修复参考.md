# 🔧 入库模块快速修复参考

## 问题
❌ 设计方案使用 `action: 'add'`,但云函数只支持 `action: 'create'`

## 解决方案
✅ 添加 `'add'` 作为 `'create'` 的别名

## 修改的文件
```
cloudfunctions/inRecords/index.js   ✅
cloudfunctions/outRecords/index.js  ✅
```

## 修改内容
```javascript
// 在 switch(action) 中添加:
case 'create':
case 'add':  // ← 新增这一行
  return await createRecord(data, wxContext)
```

## 使用方法
```javascript
// 现在两种方式都支持:
action: 'add'     // ✅ 推荐(符合设计方案)
action: 'create'  // ✅ 兼容(旧代码)
```

## 部署
```
微信开发者工具 → 云函数 → 右键上传
- inRecords
- outRecords
```

## 验证
```javascript
// 测试代码
const result = await this.$api.callFunction('inRecords', {
  action: 'add',
  data: { recordNo: 'TEST001', ... }
})
```

---
**状态**: ✅ 修复完成 | **待部署**: ⏳


