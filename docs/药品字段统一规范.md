# 药材字段统一规范

## 📋 统一字段定义

所有药材相关操作（入库、出库、门诊登记）必须使用以下统一字段：

### 核心字段

| 字段名 | 类型 | 说明 | 示例 | 必填 |
|--------|------|------|------|------|
| `drugId` | String | 系统内部ID（主键） | `cloud://drug_abc123xyz` | ✅ |
| `drugCode` | String | 药材代码（业务编码） | `AMOX500-TAB` | ⚠️ 推荐 |
| `drugName` | String | 药材名称 | `阿莫西林胶囊` | ✅ |
| `specification` | String | 规格 | `0.25g*24粒` | ✅ |
| `unit` | String | 单位 | `盒` | ✅ |
| `manufacturer` | String | 生产厂家 | `XX制药有限公司` | ⚠️ 推荐 |

### 扩展字段

| 字段名 | 类型 | 说明 | 示例 | 必填 |
|--------|------|------|------|------|
| `category` | String | 分类 | `抗生素` | ⭕ |
| `minUnit` | String | 最小单位 | `粒` | ⭕ |
| `packUnit` | String | 包装单位 | `盒` | ⭕ |
| `conversionRate` | Number | 换算率 | `24` | ⭕ |
| `isHighValue` | Boolean | 是否高值药材 | `false` | ⭕ |
| `isEmergency` | Boolean | 是否急救药材 | `false` | ⭕ |

---

## 🚫 废弃字段

以下字段已废弃，不应继续使用：

| 废弃字段 | 替换为 | 说明 |
|---------|--------|------|
| `spec` | `specification` | 统一使用完整命名 |
| `code` | `drugCode` | 统一使用前缀命名 |
| `name` | `drugName` | 统一使用前缀命名（仅数据库字段） |

---

## 📦 数据结构示例

### 入库单药材项

```javascript
{
  drugId: "cloud://drug_abc123xyz",
  drugCode: "AMOX500-TAB",
  drugName: "阿莫西林胶囊",
  specification: "0.25g*24粒",
  unit: "盒",
  manufacturer: "XX制药有限公司",
  category: "抗生素",
  batch: "20250101",
  productionDate: "2025-01-01",
  expireDate: "2027-01-01",
  quantity: 100,
  price: 25.50,
  isHighValue: false,
  isEmergency: false
}
```

### 出库单药材项

```javascript
{
  drugId: "cloud://drug_abc123xyz",
  drugCode: "AMOX500-TAB",
  drugName: "阿莫西林胶囊",
  specification: "0.25g*24粒",
  unit: "盒",
  manufacturer: "XX制药有限公司",
  batch: "20250101",
  batchId: "cloud://batch_xyz789",
  expireDate: "2027-01-01",
  quantity: 10,
  price: 25.50,
  isHighValue: false,
  isEmergency: false
}
```

### 门诊登记药材项

```javascript
{
  drugId: "cloud://drug_abc123xyz",
  drugCode: "AMOX500-TAB",
  drugName: "阿莫西林胶囊",
  specification: "0.25g*24粒",
  unit: "盒",
  quantity: 2,
  batchId: "cloud://batch_xyz789",
  batch: "20250101",
  location: "land_park",
  minUnit: "粒",
  packUnit: "盒",
  conversionRate: 24
}
```

---

## 🔄 字段兼容性处理

在读取药材数据时，需要兼容旧数据：

```javascript
// ✅ 正确的兼容性处理
{
  drugId: drugInfo._id,
  drugCode: drugInfo.drugCode || drugInfo.code || '',
  drugName: drugInfo.name || drugInfo.drugName || '未知药材',
  specification: drugInfo.specification || drugInfo.spec || '',
  unit: drugInfo.unit || drugInfo.unitName || '',
  manufacturer: drugInfo.manufacturer || drugInfo.manufacturerName || ''
}
```

---

## 📝 更新记录

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2025-11-04 | v1.0 | 初始版本，统一字段规范 |

---

## ✅ 检查清单

在修改代码时，请确保：

- [ ] 使用 `specification` 而不是 `spec`
- [ ] 使用 `drugCode` 而不是 `code`
- [ ] 所有提交数据包含 `drugId` 和 `drugCode`
- [ ] 兼容性处理正确（支持旧字段）
- [ ] 数据库查询和写入使用统一字段
- [ ] UI 显示使用统一字段命名

