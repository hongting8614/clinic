# 入库/出库报表字段设计完成

## ✅ 已完成的修改

### 📥 入库明细报表字段（13列）

#### Excel & PDF 导出字段：
1. **序号** - 自动编号
2. **单号** - 入库单号
3. **入库日期** - 入库时间
4. **药材名称** - 药品名称
5. **规格** - 药品规格
6. **单位** - 包装单位
7. **批号** - 生产批号
8. **生产日期** - 生产日期
9. **有效期** - 失效日期
10. **生产厂家** - 厂家名称
11. **批准文号** - 药品批准文号
12. **数量** - 入库数量（最小单位）
13. **操作人** - 入库操作人

#### 已删除字段：
- ❌ 条形码（barcode）
- ❌ 分类（category）
- ❌ 价格（price）
- ❌ 金额（amount）

### 📤 出库明细报表字段（12列）

#### Excel & PDF 导出字段：
1. **序号** - 自动编号
2. **单号** - 出库单号
3. **出库日期** - 出库时间
4. **园区** - 发往园区（陆园/水园）
5. **药材名称** - 药品名称
6. **规格** - 药品规格
7. **单位** - 包装单位
8. **批号** - 生产批号
9. **生产日期** - 生产日期
10. **有效期** - 失效日期
11. **生产厂家** - 厂家名称
12. **数量** - 出库数量（最小单位）
13. **发放人** - 出库操作人

#### 已删除字段：
- ❌ 条形码（barcode）
- ❌ 分类（category）
- ❌ 批准文号（approvalNumber）- 出库不需要
- ❌ 价格（price）
- ❌ 金额（amount）

## 📋 前端页面字段（已同步）

### 入库报表前端（pages-sub/report/inbound.vue）
- ✅ 显示字段：单号、入库日期、药材名称、规格、单位、批号、生产日期、有效期、生产厂家、批准文号、数量、操作人
- ✅ 高值/急救标签显示
- ✅ 已删除条形码和分类显示

### 出库报表前端（pages-sub/report/outbound.vue）
- ✅ 显示字段：单号、出库日期、园区、药材名称、规格、单位、批号、生产日期、有效期、生产厂家、数量、发放人
- ✅ 高值/急救标签显示
- ✅ 已删除条形码和分类显示
- ✅ 园区字段使用 renderLocation 方法显示

## 🔧 云函数更新状态

### 已更新部分：
1. ✅ **exportInboundPDF** - PDF导出已更新为13列（包含批准文号，不含条形码和分类）
2. ✅ **inboundDetailReport** - 数据查询包含所有必要字段

### ⚠️ 待更新部分：
1. **exportInboundExcel** - Excel导出表头需要更新
   - 当前：14列（包含条形码、分类）
   - 目标：13列（删除条形码和分类，保留批准文号）

2. **exportOutboundExcel** - Excel导出需要完整实现
   - 当前：14列（包含条形码、分类、价格、金额）
   - 目标：13列（序号+单号+出库日期+园区+药材名称+规格+单位+批号+生产日期+有效期+生产厂家+数量+发放人）

3. **exportOutboundPDF** - PDF导出需要完整实现
   - 当前：13列（包含价格、金额）
   - 目标：12列（删除价格、金额，添加园区字段）

4. **outboundDetailReport** - 数据查询需要添加字段
   - 需要添加：outboundDate（出库日期）、toLocation（园区）、approvalNumber（批准文号）、isHighValue、isEmergency

## 📊 报表格式规范

### Excel格式：
- **纸张**：A4横向
- **标题**：黑体 22号 加粗
- **表头**：仿宋_GB2312 12-14号 加粗，灰色背景
- **数据**：仿宋_GB2312 11-12号
- **合计行**：仿宋_GB2312 10号 加粗，黄色背景

### PDF格式：
- **纸张**：A4横向
- **标题**：黑体 22号
- **表头**：仿宋 14号
- **数据**：仿宋 12号
- **表格居中显示**

## 🎯 下一步操作

需要更新云函数 `cloudfunctions/reports/index.js` 中的以下函数：

1. **exportInboundExcel** - 更新明细模式表头和列定义（删除条形码和分类）
2. **exportOutboundExcel** - 完整实现出库Excel导出（13列，包含园区）
3. **exportOutboundPDF** - 完整实现出库PDF导出（12列，包含园区）
4. **outboundDetailReport** - 添加缺失字段到数据查询

## 📝 字段对应关系

### 数据库字段 → 报表显示
- `recordNo` → 单号
- `createTime` / `inboundDate` → 入库日期
- `createTime` / `outboundDate` → 出库日期
- `drugName` / `name` → 药材名称
- `specification` / `spec` → 规格
- `unit` / `packUnit` → 单位
- `batch` / `batchNo` → 批号
- `productionDate` → 生产日期
- `expireDate` → 有效期
- `manufacturer` → 生产厂家
- `approvalNumber` → 批准文号（仅入库）
- `quantity` → 数量
- `operator` → 操作人/发放人
- `toLocation` / `toLocationName` → 园区（仅出库）

---

**更新时间**：2026-01-17
**状态**：前端已完成，云函数部分待更新

