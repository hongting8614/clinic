# PowerShell 终端命令指南

## ✅ 问题已解决！

经过测试，您的 PowerShell 终端现在可以正常读取和执行代码了！

### 测试结果
- ✅ PowerShell 版本：5.1.19041.6456
- ✅ Node.js 版本：v22.19.0
- ✅ npm 版本：10.9.3
- ✅ 当前目录：D:\AK-PMS
- ✅ 可以读取 Vue 组件代码
- ✅ 可以读取云函数代码
- ✅ 可以读取工具函数代码
- ✅ 项目文件统计：18,959 个 JS/Vue 文件
- ✅ 可以执行复杂的 PowerShell 命令

## 问题说明
PowerShell 不支持 bash 风格的 `&&` 命令分隔符，需要使用 PowerShell 语法。

## 解决方案

### 1. 使用分号分隔命令
```powershell
# ❌ 错误（bash 风格）
cd /d D:\AK-PMS && dir

# ✅ 正确（PowerShell 风格）
cd D:\AK-PMS; dir
```

### 2. 使用条件执行
```powershell
# 只有前一个命令成功才执行下一个
cd D:\AK-PMS; if ($?) { npm install }
```

### 3. 使用批处理文件（推荐）
已为您创建以下快捷脚本：

#### 开发服务器
```bash
.\dev.bat
```

#### 构建生产版本
```bash
.\build.bat
```

#### 快速推送到 Git
```bash
.\快速推送.bat
```

#### 清理项目
```bash
.\清理项目.bat
```

## 常用 PowerShell 命令对照表

| Bash 命令 | PowerShell 命令 | 说明 |
|-----------|----------------|------|
| `ls` | `dir` 或 `Get-ChildItem` | 列出文件 |
| `cd /d D:\path` | `cd D:\path` | 切换目录 |
| `cat file.txt` | `Get-Content file.txt` | 查看文件内容 |
| `rm file.txt` | `Remove-Item file.txt` | 删除文件 |
| `mkdir dir` | `New-Item -ItemType Directory dir` | 创建目录 |
| `cmd1 && cmd2` | `cmd1; if ($?) { cmd2 }` | 条件执行 |
| `cmd1 \|\| cmd2` | `cmd1; if (!$?) { cmd2 }` | 失败时执行 |

## 项目常用命令

### 启动开发服务器
```powershell
npm run dev:mp-weixin
```

### 构建生产版本
```powershell
npm run build:mp-weixin
```

### 安装依赖
```powershell
npm install
```

### 查看项目结构
```powershell
dir
```

### 查看 Git 状态
```powershell
git status
```

### 提交代码
```powershell
git add .
git commit -m "提交说明"
git push
```

## 快速测试终端

运行以下命令测试终端是否正常：

```powershell
# 测试 1: 查看当前目录
pwd

# 测试 2: 列出文件
dir

# 测试 3: 查看 Node 版本
node -v

# 测试 4: 查看 npm 版本
npm -v

# 测试 5: 多命令执行
cd D:\AK-PMS; pwd; dir package.json
```

## 提示

1. **不要使用 `/d` 参数**：PowerShell 的 `cd` 命令不需要 `/d` 参数
2. **使用分号**：用 `;` 分隔多个命令
3. **使用批处理文件**：对于复杂的命令序列，创建 `.bat` 文件更方便
4. **检查路径**：确保路径使用反斜杠 `\` 或正斜杠 `/`（PowerShell 两者都支持）

## 如果还有问题

如果终端仍然无法正常工作，可以尝试：

1. **重启终端**：关闭并重新打开终端窗口
2. **使用 CMD**：在 Cursor 设置中切换到 CMD 终端
3. **使用 Git Bash**：安装 Git for Windows 后使用 Git Bash
4. **检查权限**：确保有足够的权限执行命令

