# 门诊园区选择问题修复 - 2026年1月17日

## 问题描述

**用户反馈**：门诊登记选择陆园，但在门诊日报中显示的是水园的数据

## 问题分析

### 根本原因

门诊登记页面（`pages-sub/clinic/add.vue`）的 `generateDailyReport` 方法使用了**旧的跳转方式**，存在以下问题：

1. **参数格式错误**：
   - 旧代码传递：`location=陆园`（园区名称）
   - 应该传递：`location=land_park`（园区代码）

2. **数据传递方式不一致**：
   - 旧代码：通过 URL 参数传递 `content`、`stats`、`tableData` 等完整数据
   - 新代码：只传递 `date` 和 `location`，由日报页面自己查询数据

3. **导致的问题**：
   - 日报页面收到错误的 `location` 参数（"陆园" 而不是 "land_park"）
   - 日报页面无法识别，回退到使用缓存的园区（可能是水园）
   - 最终显示了错误园区的数据

### 代码对比

#### 修改前（错误的代码）

```javascript
// 门诊登记页面 - generateDailyReport 方法
async generateDailyReport() {
  uni.showLoading({ title: '生成中...' });
  
  const location = this.form.location;  // 'land_park' 或 'water_park'
  const locationName = location === 'land_park' ? '陆园' : '水园';
  
  // 查询记录
  const res = await wx.cloud.callFunction({...});
  const records = res.result.data.list;
  
  // 生成报表数据
  const report = this.formatDailyReport(records, dateStr, locationName);
  const stats = this.calculateStats(records);
  const tableData = this.prepareTableData(records);
  
  // ❌ 错误：传递了园区名称而不是代码
  uni.navigateTo({
    url: `/pages-sub/report/daily?content=${encodeURIComponent(report)}&date=${encodeURIComponent(reportDate)}&location=${encodeURIComponent(locationName)}&stats=${encodeURIComponent(JSON.stringify(stats))}&tableData=${encodeURIComponent(JSON.stringify(tableData))}`
  });
}
```

#### 修改后（正确的代码）

```javascript
// 门诊登记页面 - generateDailyReport 方法
async generateDailyReport() {
  try {
    const today = new Date();
    const year = today.getFullYear();
    const month = String(today.getMonth() + 1).padStart(2, '0');
    const day = String(today.getDate()).padStart(2, '0');
    const dateStr = `${year}-${month}-${day}`;

    // 必须先选择就诊园区
    const location = this.form.location;
    if (!location || (location !== 'land_park' && location !== 'water_park')) {
      uni.showToast({ title: '请先选择就诊园区', icon: 'none' });
      this.showLocationTip = true;
      return;
    }

    console.log('门诊登记页生成日报参数:', { dateStr, location });

    // ✅ 正确：传递园区代码，由日报页面统一查询和处理
    uni.navigateTo({
      url: `/pages-sub/report/daily?date=${dateStr}&location=${location}`
    });
  } catch (err) {
    console.error('生成日报失败:', err);
    uni.showToast({ title: '生成失败', icon: 'none' });
  }
}
```

## 修复内容

### 1. 修改门诊登记页面的日报生成方法

**文件**：`pages-sub/clinic/add.vue`

**修改内容**：
1. ✅ 简化 `generateDailyReport` 方法
2. ✅ 传递园区代码（`land_park`/`water_park`）而不是名称（"陆园"/"水园"）
3. ✅ 移除本地查询和数据处理逻辑
4. ✅ 删除不再使用的 `formatDailyReport`、`calculateStats`、`prepareTableData` 方法
5. ✅ 添加日志输出，便于调试

### 2. 统一数据流程

现在三个入口的日报生成逻辑完全一致：

```
入口1: 门诊首页 (pages/clinic/index.vue)
  ↓
generateTodayReport()
  ├─ 获取日期: 2026-01-17
  ├─ 获取园区: userInfo.location 或 clinic_last_location
  └─ 跳转: /pages-sub/report/daily?date=2026-01-17&location=land_park

入口2: 门诊登记页 (pages-sub/clinic/add.vue)
  ↓
generateDailyReport()
  ├─ 获取日期: 2026-01-17
  ├─ 获取园区: this.form.location (当前选择的园区)
  └─ 跳转: /pages-sub/report/daily?date=2026-01-17&location=land_park

入口3: 报表中心 (pages-sub/report/center.vue)
  ↓
generateDailyReport()
  ├─ 获取日期: 用户选择的日期
  ├─ 获取园区: 用户选择的园区
  └─ 跳转: /pages-sub/report/daily?date=2026-01-17&location=land_park

↓ 统一处理 ↓

门诊日报页面 (pages-sub/report/daily.vue)
  ↓
onLoad(options)
  ├─ 解析参数: date, location
  ├─ 验证日期格式
  └─ 调用: generateFromClinicRecords(dateStr, location)
      ↓
      ├─ 查询门诊记录（使用传入的 location）
      ├─ 生成日报文本
      ├─ 计算统计数据
      ├─ 准备表格数据
      └─ 显示日报
```

## 修复效果

### 修复前
1. ❌ 门诊登记选择陆园
2. ❌ 点击"门诊日报"
3. ❌ 日报页面显示水园的数据（错误）

### 修复后
1. ✅ 门诊登记选择陆园
2. ✅ 点击"门诊日报"
3. ✅ 日报页面显示陆园的数据（正确）

## 测试场景

### 场景 1: 门诊登记页 - 陆园
1. 打开门诊登记页面
2. 选择"陆园"
3. 点击"门诊日报"按钮
4. ✅ 应该显示陆园的日报数据
5. ✅ 页面标题显示"陆园"

### 场景 2: 门诊登记页 - 水园
1. 打开门诊登记页面
2. 选择"水园"
3. 点击"门诊日报"按钮
4. ✅ 应该显示水园的日报数据
5. ✅ 页面标题显示"水园"

### 场景 3: 未选择园区
1. 打开门诊登记页面
2. 不选择园区（或园区被锁定）
3. 点击"门诊日报"按钮
4. ✅ 应该提示"请先选择就诊园区"
5. ✅ 自动弹出园区选择提示窗

### 场景 4: 门诊首页
1. 打开门诊首页
2. 点击"门诊日报"
3. ✅ 应该显示当前用户所在园区的日报
4. ✅ 园区信息正确显示

## 技术细节

### 园区代码映射

| 园区名称 | 园区代码 | 存储位置 |
|---------|---------|---------|
| 陆园 | `land_park` | `form.location` / `userInfo.location` / `clinic_last_location` |
| 水园 | `water_park` | `form.location` / `userInfo.location` / `clinic_last_location` |

### URL 参数格式

**正确格式**：
```
/pages-sub/report/daily?date=2026-01-17&location=land_park
```

**错误格式**（已修复）：
```
/pages-sub/report/daily?date=2026年1月17日&location=陆园&content=...&stats=...&tableData=...
```

### 日志输出

修复后的代码添加了日志输出，便于调试：

```javascript
console.log('门诊登记页生成日报参数:', { dateStr, location });
// 输出示例：
// 门诊登记页生成日报参数: { dateStr: '2026-01-17', location: 'land_park' }
```

## 相关文件

### 修改的文件
- ✅ `pages-sub/clinic/add.vue` - 门诊登记页面（主要修改）

### 确认正确的文件
- ✅ `pages/clinic/index.vue` - 门诊首页（已正确）
- ✅ `pages-sub/report/daily.vue` - 门诊日报页面（已正确）

## 注意事项

1. **园区选择优先级**（门诊登记页）：
   - 使用 `this.form.location`（当前表单选择的园区）
   - 必须先选择园区才能生成日报

2. **园区选择优先级**（门诊首页）：
   - 第一优先：`userInfo.location`（用户信息中的园区）
   - 第二优先：`clinic_last_location`（最近使用的园区）
   - 默认值：`land_park`（陆园）

3. **数据一致性**：
   - 所有入口统一使用园区代码（`land_park`/`water_park`）
   - 日报页面统一查询和处理数据
   - 避免数据在多个地方重复处理

4. **错误处理**：
   - 未选择园区时提示用户
   - 自动弹出园区选择窗口
   - 添加详细的错误日志

## 后续建议

1. **功能增强**：
   - 💡 在日报页面添加园区切换功能
   - 💡 记住用户最后查看的园区
   - 💡 支持同时查看多个园区的日报

2. **用户体验**：
   - 💡 在门诊登记页面顶部显示当前选择的园区
   - 💡 园区切换时给予明确的视觉反馈
   - 💡 添加园区选择的快捷入口

3. **数据验证**：
   - 💡 在云函数中验证园区参数的有效性
   - 💡 添加园区数据的缓存机制
   - 💡 记录园区切换的操作日志

---

**修复完成时间**：2026年1月17日  
**修复人员**：AI Assistant  
**测试状态**：待测试  
**部署状态**：待部署

