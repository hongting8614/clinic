# 门诊查询功能分析与优化方案 - 2026年1月17日

## 现状分析

### 1. 首页"门诊查询"功能

**跳转路径**：`/pages-sub/report/clinic`

**当前功能**：
- ✅ 有园区选择（全部园区/陆园/水园）
- ✅ 有日期范围筛选
- ✅ 有姓名模糊查询
- ✅ 有Excel导出功能
- ✅ 有PDF导出功能
- ✅ 卡片式展示门诊记录

**页面标题**：门诊登记报表

### 2. 发现的问题

#### 问题1：存在重复页面
- **`pages-sub/report/clinic.vue`** - 门诊登记报表（功能完整）
- **`pages-sub/report/record.vue`** - 门诊病历查询（功能类似，但没有导出）

两个页面功能高度重复，应该合并或删除其中一个。

#### 问题2：首页导出功能与门诊查询功能重复

**首页（`pages/clinic/index.vue`）**：
- 有"快速导出"区域
- 点击"门诊登记表"按钮
- 弹出日期选择对话框
- 可以导出Excel/PDF

**门诊查询页面（`pages-sub/report/clinic.vue`）**：
- 有筛选区域（日期、姓名、园区）
- 点击"导出"按钮
- 可以导出Excel/PDF

**问题**：两个地方都可以导出门诊登记表，功能重复。

## 优化方案

### 方案A：保留门诊查询页面的导出功能，简化首页

**优点**：
- 门诊查询页面功能更完整（可以先筛选再导出）
- 首页更简洁
- 用户体验更好（先查看数据，确认无误后再导出）

**修改内容**：
1. ✅ 保留 `pages-sub/report/clinic.vue`（门诊查询页面）
2. ❌ 删除 `pages-sub/report/record.vue`（门诊病历查询页面，功能重复）
3. ✅ 保留首页的"快速导出"功能（方便快速导出当天/本周/本月数据）
4. ✅ 首页的"门诊查询"继续跳转到 `pages-sub/report/clinic.vue`

### 方案B：删除首页的导出功能，统一到门诊查询页面

**优点**：
- 功能集中，避免重复
- 首页更简洁

**缺点**：
- 用户需要多点击一次才能导出

**修改内容**：
1. ✅ 保留 `pages-sub/report/clinic.vue`（门诊查询页面）
2. ❌ 删除 `pages-sub/report/record.vue`（门诊病历查询页面）
3. ❌ 删除首页的"快速导出"区域
4. ✅ 首页的"门诊查询"继续跳转到 `pages-sub/report/clinic.vue`

## 推荐方案：方案A（保留两个入口）

### 理由

1. **用户场景不同**：
   - 首页"快速导出"：快速导出今天/本周/本月的数据，无需查看
   - 门诊查询"导出"：先筛选查看数据，确认无误后再导出

2. **功能互补**：
   - 首页导出：快速、便捷
   - 查询页导出：精确、可控

3. **用户体验好**：
   - 不同场景使用不同入口
   - 减少操作步骤

## 实施步骤

### 步骤1：删除重复的门诊病历查询页面

**删除文件**：
- `pages-sub/report/record.vue`

**修改文件**：
- `pages.json` - 删除 `record` 页面配置

### 步骤2：优化首页的导出功能

**当前问题**：
- 首页导出时没有明确显示园区选择
- 使用的是 `userInfo.location`，可能不准确

**优化方案**：
- 在导出对话框中添加园区选择
- 默认选择当前用户的园区
- 允许用户切换园区

### 步骤3：优化门诊查询页面

**当前功能**：✅ 已经很完善

**可选优化**：
- 添加"诊断关键字"筛选（类似 record.vue）
- 添加"出诊/门诊"类型筛选
- 添加分页功能（当前最多显示1000条）

## 详细修改内容

### 修改1：删除重复页面

#### 删除文件
```bash
删除：pages-sub/report/record.vue
```

#### 修改 pages.json
```json
{
  "root": "pages-sub/report",
  "pages": [
    // ... 其他页面
    {
      "path": "clinic",
      "style": {
        "navigationBarTitleText": "门诊查询"  // 改为更通用的名称
      }
    }
    // ❌ 删除这个配置
    // {
    //   "path": "record",
    //   "style": {
    //     "navigationBarTitleText": "门诊病历查询"
    //   }
    // }
  ]
}
```

### 修改2：优化首页导出对话框（添加园区选择）

#### 修改 pages/clinic/index.vue

**添加数据**：
```javascript
data() {
  return {
    // ... 现有数据
    exportLocation: 'land_park',  // 导出时选择的园区
    locationOptions: ['陆园', '水园']
  }
}
```

**修改模板**：
```vue
<!-- 导出时间段选择弹窗 -->
<view v-if="showDatePicker" class="date-picker-mask" @tap="closeDatePicker">
  <view class="date-picker-dialog" @tap.stop>
    <view class="dialog-title">选择导出时间段</view>
    
    <!-- 添加园区选择 -->
    <view class="date-range-section">
      <view class="date-item">
        <text class="date-label">园区</text>
        <picker mode="selector" :range="locationOptions" :value="exportLocationIndex" @change="onExportLocationChange">
          <view class="date-value">{{ locationOptions[exportLocationIndex] }}</view>
        </picker>
      </view>
    </view>
    
    <view class="date-range-section">
      <!-- 现有的日期选择 -->
    </view>
    
    <!-- 其他内容保持不变 -->
  </view>
</view>
```

### 修改3：优化门诊查询页面标题

#### 修改 pages-sub/report/clinic.vue

```javascript
computed: {
  reportTitle() {
    return '门诊查询'  // 简化标题
  }
}
```

## 功能对比表

| 功能 | 首页"快速导出" | 门诊查询页面 | 门诊病历查询（待删除） |
|------|---------------|-------------|---------------------|
| 园区选择 | ⚠️ 需要添加 | ✅ 有 | ✅ 有 |
| 日期范围 | ✅ 有 | ✅ 有 | ✅ 有 |
| 姓名筛选 | ❌ 无 | ✅ 有 | ✅ 有 |
| 诊断筛选 | ❌ 无 | ❌ 无 | ✅ 有 |
| 数据预览 | ❌ 无 | ✅ 有 | ✅ 有 |
| Excel导出 | ✅ 有 | ✅ 有 | ❌ 无 |
| PDF导出 | ✅ 有 | ✅ 有 | ❌ 无 |
| 快捷日期 | ✅ 有（今天/本周/本月） | ❌ 无 | ❌ 无 |

## 用户使用场景

### 场景1：快速导出今天的数据
**操作流程**：
1. 打开门诊首页
2. 点击"门诊登记表"
3. 选择园区（新增）
4. 点击"今天"
5. 点击"导出Excel"

**优点**：快速、便捷，3步完成

### 场景2：查看并导出特定患者的记录
**操作流程**：
1. 打开门诊首页
2. 点击"门诊查询"
3. 输入患者姓名
4. 选择日期范围
5. 点击"查询"
6. 查看记录列表
7. 点击"导出"

**优点**：可以先查看数据，确认无误后再导出

### 场景3：导出本周所有记录
**操作流程**：
1. 打开门诊首页
2. 点击"门诊登记表"
3. 选择园区（新增）
4. 点击"本周"
5. 点击"导出Excel"

**优点**：快速、便捷

## 总结

### 需要删除的文件
- ❌ `pages-sub/report/record.vue`（门诊病历查询页面，功能与clinic.vue重复）

### 需要修改的文件
- ✅ `pages.json`（删除record页面配置，修改clinic页面标题）
- ✅ `pages/clinic/index.vue`（添加园区选择到导出对话框）
- ⚠️ `pages-sub/report/clinic.vue`（可选：添加诊断筛选功能）

### 保留的功能
- ✅ 首页"快速导出"（添加园区选择后）
- ✅ 门诊查询页面（功能完整，保留）

---

**分析完成时间**：2026年1月17日  
**分析人员**：AI Assistant  
**建议方案**：方案A（保留两个入口，优化首页导出对话框）

